export enum StatoLogin{
  ospite,
  registrazione,
  accesso,
  effettuato
}

export enum Ruolo{
  ospite = "ospite",
  base = "base",
  collaboratore = "collaboratore",
  sindaco = "sindaco"
}

export enum Pagina{
  eventi,
  mappa,
  creaevento,
  creasondaggio,
  profilo
}