export enum StatoLogin{
  ospite,
  registrazione,
  accesso,
  effettuato
}

export enum Ruolo{
  ospite = "ospite",
  base = "",
  collaboratore = "collaboratore",
  sindaco = "sindaco"
}

export enum Pagina{
  eventi,
  mappa,
  creaevento,
  creasondaggio
}